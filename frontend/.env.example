# Vite requires VITE_ prefix for client-side exposure
VITE_API_BASE_URL=https://api.yourdomain.com     # prod
# staging example: https://api-stage.yourdomain.com
# preview example: use the auto-assigned backend preview URL